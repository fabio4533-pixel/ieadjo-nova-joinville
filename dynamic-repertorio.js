(()=>{
if(window.__ieadjoDynamicRepertorio)return;window.__ieadjoDynamicRepertorio=true;
const SB_URL='https://fzpffyouubmbxfwycywg.supabase.co';
const SB_KEY='sb_publishable_fqG3I5cRWBXjn-EP0FoATA_0-SWvfsR';
let songs=[],schedules=[],loading=false,lastPayload='';
function getSession(){try{return JSON.parse(localStorage.getItem('ieadjo_member_session')||'null')}catch{return null}}
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function todayISO(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function setText(el,text){if(el&&el.textContent!==text)el.textContent=text}
function updateHomeCounters(){const view=document.getElementById('view');if(!view)return;const rep=view.querySelector('[data-go="repertorio"] small');setText(rep,`${songs.length} ${songs.length===1?'música':'músicas'}`);const escBtn=view.querySelector('[data-go="escalas"] small');if(escBtn){const n=schedules.filter(s=>String(s.event_date)>=todayISO()).length;setText(escBtn,`${n} ${n===1?'próxima':'próximas'}`)}}
async function fetchData(){if(loading)return;const s=getSession();if(!s?.access_token)return;loading=true;try{const headers={apikey:SB_KEY,Authorization:'Bearer '+s.access_token};const [songsRes,schedulesRes]=await Promise.all([
fetch(SB_URL+'/rest/v1/ieadjo_songs?select=id,title,song_key,notes,service_day,schedule_id,cifra_url,letra_url,created_at&order=created_at.desc',{cache:'no-store',headers}),
fetch(SB_URL+'/rest/v1/ieadjo_schedules?select=id,event_date,title,time_text&event_date=gte.'+todayISO()+'&order=event_date.asc,id.asc',{cache:'no-store',headers})
]);if(!songsRes.ok)throw new Error('Falha ao carregar repertório');if(!schedulesRes.ok)throw new Error('Falha ao carregar escalas');const nextSongs=await songsRes.json();const nextSchedules=await schedulesRes.json();const payload=JSON.stringify([nextSongs,nextSchedules]);songs=nextSongs;schedules=nextSchedules;updateHomeCounters();if(payload!==lastPayload){lastPayload=payload;renderIfOpen(true)}else renderIfOpen(false)}catch(e){console.warn('Atualização dinâmica:',e)}finally{loading=false}}
function spotifyUrl(t){return 'https://open.spotify.com/search/'+encodeURIComponent(t)}
function cifraSearch(t,letter=false){return 'https://www.cifraclub.com.br/search/?q='+encodeURIComponent(t+(letter?' letra':''))}
function safeUrl(v,fallback){try{const u=new URL(v||'');return /^https?:$/.test(u.protocol)?u.href:fallback}catch{return fallback}}
function button(label,url,bg){return `<a href="${esc(url)}" target="_blank" rel="noopener noreferrer external" style="display:block;text-decoration:none;text-align:center;border-radius:10px;background:${bg};color:#fff;padding:9px 10px;font-size:11px;font-weight:800;white-space:nowrap">${label}</a>`}
function renderIfOpen(force=false){const view=document.getElementById('view');if(!view)return;updateHomeCounters();const h=view.querySelector('.pagehead h1');if(!h||h.textContent.trim()!=='Repertório')return;if(!force&&view.querySelector('.dynamic-song,.dynamic-empty'))return;view.querySelectorAll('.song,.dynamic-song,.dynamic-empty').forEach(x=>x.remove());const head=view.querySelector('.pagehead');if(!songs.length){head.insertAdjacentHTML('afterend','<div class="card dynamic-empty"><div class="grow"><b>Nenhuma música cadastrada.</b><span>Quando a liderança adicionar no painel, ela aparecerá aqui automaticamente.</span></div></div>');return}
const html=songs.map(s=>{const cifra=safeUrl(s.cifra_url,cifraSearch(s.title,false)),letra=safeUrl(s.letra_url,cifraSearch(s.title,true));return `<div class="card dynamic-song" data-dynamic-repertorio="1" style="display:flex;gap:12px;align-items:center"><div style="width:42px;height:42px;border-radius:12px;background:#eef4f9;display:grid;place-items:center;font-size:22px;color:#174f7c;flex:none">♫</div><div class="grow"><b>${esc(s.title)}</b><span>Tom: ${esc(s.song_key||'-')} • ${esc(s.service_day||'Culto')}</span>${s.notes?`<span>${esc(s.notes)}</span>`:''}</div><div style="display:flex;flex-direction:column;gap:6px;flex:none">${button('▶ Spotify',spotifyUrl(s.title),'#1db954')}${button('🎸 Cifra',cifra,'#0e2a47')}${button('📄 Letra',letra,'#174f7c')}</div></div>`}).join('');
head.insertAdjacentHTML('afterend',html)}
function watch(){const view=document.getElementById('view');if(!view){setTimeout(watch,100);return}new MutationObserver(()=>{updateHomeCounters();const h=view.querySelector('.pagehead h1');if(h?.textContent.trim()==='Repertório'&&!view.querySelector('.dynamic-song,.dynamic-empty'))setTimeout(()=>renderIfOpen(false),30)}).observe(view,{childList:true,subtree:false});updateHomeCounters();renderIfOpen(false)}
watch();fetchData();setInterval(fetchData,10000);
})();